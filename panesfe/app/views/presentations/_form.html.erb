<%= form_for @presentation, :html => { :class => "form presentation", :role => 'form' } do |f| %>

    <% if @presentation.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@presentation.errors.count, "error") %> prohibited this presentation from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @presentation.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label' %>
    <%= f.text_field :name, :class => 'form-control' %>
    <%= error_span(@presentation[:name]) %>
  </div>

<!--   <div class="form-group">
    <div class="checkbox">
      <%= label_tag do %>
        <%= f.check_box :published %>
        <%= Presentation.human_attribute_name(:published) %>
      <%end%>
      <%= error_span(@presentation[:published]) %>
    </div>
  </div>

 -->
  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            presentations_path, :class => 'btn btn-default' %>

<% end %>

<%= if @presentation.id then render(partial: 'images_form') end%>
